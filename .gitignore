node_modules/
dist/
coverage/
*.log
.DS_Store
.env
.env.*
tmp/
out/

# local agent scratch (hooks) — but TRACK the generated project index so other
# clones / CI / future sessions get project-relevant SessionStart grounding (#772).
.ai-workspace/*
!.ai-workspace/PROJECT-INDEX.md
!.ai-workspace/PROJECT-INDEX-reference.md
# TRACK the in-repo posts archive mirror so a fresh clone / CI has the canonical
# post copy + index in git too (the external durable archive lives outside the repo, #821).
!.ai-workspace/posts/
!.ai-workspace/posts/**
